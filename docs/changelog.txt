=== Ver 50.25 (49.25) ===

* В агентах сателлитов реализована поддержка pid-файлов, благодаря чему
появилась возможность посылать сигналы перезагрузки или завершения работы
скриптов.

Рестарт noserver.pl:
kill -HUP `cat /var/run/noserver.pid`

Завершение работы noserver.pl:
kill -TERM `cat /var/run/noserver.pid`

Пока pid-файлы поддерживается только noserver.pl (noserver.pid) и
nol2auth.pl (nol2auth.pid).

Посылать сигналы не является необходимостью - разработаны rc.d скрипты,
которые поддерживают команды start/stop/restart. Эти скрипты находятся
в папке /usr/local/nodeny/rc.d. Скопируйте их в системную rc.d:

cp /usr/local/nodeny/rc.d/* /usr/local/etc/rc.d/

Предусмотрена защита от параллельного запуска, а также зомби pid-файлов -
когда pid-файл существует, а процесс - нет (это возможно если послать
сигнал -INT. Посылайте -TERM чтоб этого не происходило).


* Улучшение инсталятора install.pl. Теперь во всех последующих апдейтах
после вопроса о пути к папке nodeny (/usr/local/nodeny), остальные пути
будут браться из истории предыдущих установок/апгрейдов: создается
файл history.nod, в котором сохраняются ключевые данные. Фича полезная,
когда www каталог находится по иному пути чем стандартно для apache в
freebsd, либо параметры соединения с mysql отличаются от дефолтовых.

* Слегка улучшен скрипт бекапа БД:

- теперь может запускаться из /etc/periodic/weekly
- не бекапится таблица traf_info т.к. не ключевая, но имеет значительный объем
- при инсталляции/апгрейде копируется в /usr/local/nodeny.

* noserver.pl, динамическое управление скоростями:

- теперь сохраняется соотношение вход/исход скоростей для пониженных скоростей
так же как оно (соотношение) было для начальных скоростей, прописанных в тарифе.

* В тарифах появилось поле `описание для клиентов`.

* Динамическое управление скоростями доступно для редактирования на странице
тарифов.

* Изменена структура папок документации: в корень вынесены index.html и
FAQ.html чтоб новичкам было легче найти "куда тыкать" в начало документации.

* Неключевые данные адресов клиентов перенесены в дополнительные поля, что
позволяет гибко настраивать их перечень (ввести новые, удалять ненужные,
например, добавить поля `город`, `подъезд`), тип (строковое, числовое,
да/нет и т.д), их названия. Возможность просмотреть/откатиться к любой
ревизии, т.е. к данным сущестовавшим в определенный момент времени.

* Таблица со списком клиентов настраиваема: в настройках админа появилась
возможность настроить несколько видов отображения списка клиентов, в каждом
свой список полей. В процессе отображения можно переключаться между
видами. Также можно убирать/восстанавливать колонки в процессе работы
со списком клиентов.

* При отображении списка клиентов добавлены различные сортировки.

=== Ver 50.26 (49.26) ===

* Поддержка pid-файла скриптом формирования конфигов nomake.pl.

* Усовершенствование модуля динамического управления скоростью:
  - зависимость скорости от времени суток;
  - ориентирование не только на месячный трафик, но и на стуточный.
  Внимание, использование фичи вне тестовых/ознакомительных целях
  требует лицензирования.

* Начато создание языковых файлов для клиентской статистики.

* Доработан поиск в разделе оборудования.

=== Ver 50.27 (49.27) ===

* В разделе вывода списка клиентов появились кнопки отображения
  трафика в заданных единицах измерения: мегабайт, целых мегабайт,
  килобайт, целых килобайт, байт.

* Исправлен баг, когда нельзя было осуществить поиск по любой улице.

* В выводе списка клиентов добавлена кнопка сортировки по начальному
  балансу.

* Поиск по фрагменту ip, например `0.250` найдет все ip = `xx.xx.0.250`

=== Ver 50.28 (49.28) ===

* Легкое изменение дизайна.

* Возможность указать титульные поля дополнительных данных, которые будут выведены
  в списке клиентов.

* Поиск по контракту работает.

=== Ver 50.29 (49.29) ===

* Исправлен баг, когда возникала ошибка при вставке апострофа в некоторые параметры
  настроек nodeny (меню админки: операции -> настройки).

* Создание модуля icq.

* В выводе списка клиентов добавлены кнопки отображения времени последней авторизации
  для каждого клиента, сортировки по финальному балансу и по трафику.

* В клиентской статистике клиенту не показывается список сетей, по которым идет
  проверка попадания адреса. Показывается только админу.

=== Ver 50.30 (49.30) ===

* исправлен баг в ядре, который не мог проявится ранее, но мог дать знать о себе при
  последующих модификациях ядра

=== Ver 50.31 (49.31) ===

* модуль посуточной абонплаты: разные абонплаты для разных пакетов. Использование данной
  возможности требует дополнительной оплаты авторам NoDeny!

* улучшение скрипта бекапа баз - корректно работает из crontab и /etc/periodic

* исправлен баг отображения лишней строки в клиентской статистике. Также в заголовке
  страницы теперь клиентам не показывается версия NoDeny

=== Ver 50.32 (49.33) ===

* исправлен баг, когда при создании алиасной записи клиента, снималась сумма за основное
  подключение, в случае, когда в тарифе было указано предустановленное подключение.

* при создании учетной записи, если админу доступна всего одна группа, то меню выбора
  группы пропускается и клиент создается в группе, к которой имеет доступ админ.

* в настройках тарифов предустановленные подключения выбираются из выпадающего списка.
  Ранее необходимо было вводить их номер. Также выводятся пресеты, которые можно указывать
  в тарифе.

* улучшен плагин квитанций - может отображать дополнительные данные (из таблицы
  дополнительных данных).

* в списке клиентов при поиске по улице отображаются варианты домов на улице.

* оптимизированны sql-запросы к дополнительным данным. В больших сетях будет заметен
  результат.

* исправленно несколько мелких багов.

* исправлено несколько грамматических ошибок. Если найдете еще - пишите.